{{ form_start(form) }}
    <div class="mb-3">
        {{ form_row(form.nom, {'label': 'Nom de la catégorie'}) }}
    </div>
    <button class="btn btn-success">
        <i class="bi bi-save"></i> {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
