{% extends 'admin/base_admin.html.twig' %}

{% block title %}Nouvelle Catégorie 🐾{% endblock %}

{% block admin_content %}
    <div class="mb-4">
        <a href="{{ path('admin_categorie_index') }}" class="text-muted text-decoration-none">
            <i class="fas fa-arrow-left me-1"></i>Retour à la liste
        </a>
        <h1 class="fluffy-title mt-2">Ajouter une Catégorie</h1>
    </div>

    <div class="fluffy-card p-4 mx-auto" style="max-width: 600px;">
        {{ include('admin/categorie/_form.html.twig') }}
    </div>
{% endblock %}
